# Application access password (required)
APP_PASSWORD=your-secure-password-here

# Maximum failed login attempts before IP lockout (default: 3)
MAX_LOGIN_ATTEMPTS=3

# Lockout duration in seconds after max failed attempts (default: 3600 = 1 hour)
LOCKOUT_DURATION_SECONDS=3600
